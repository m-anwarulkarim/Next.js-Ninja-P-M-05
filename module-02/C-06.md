## 📂 Route Groups কী? (The Core Concept)

Next.js-এ সাধারণত কোনো ফোল্ডার তৈরি করলে সেটি URL-এর অংশ হয়ে যায় (যেমন: `app/dashboard/page.tsx` হয়ে যায় `domain.com/dashboard`)।

কিন্তু যদি আপনি কোনো ফোল্ডারের নাম **ফার্স্ট ব্র্যাকেটের** ভেতর রাখেন—যেমন `(auth)` বা `(marketing)`—তাহলে Next.js ওই ফোল্ডারটিকে URL-এর জন্য গণনায় ধরবে না। একেই বলে **Route Group**।

### 🧪 উদাহরণ (Example):

আপনার ফোল্ডার স্ট্রাকচার যদি এমন হয়:

```text
app/
 ├─ (auth)/
 │   ├─ login/page.tsx
 │   └─ register/page.tsx
 └─ (dashboard)/
     └─ settings/page.tsx

```

👉 **URL হবে:**

- `/login` (এখানে `(auth)` উধাও)
- `/register`
- `/settings` (এখানে `(dashboard)` উধাও)

---

## ❓ কেন এটি প্রয়োজন? (Use Cases)

Route Groups মূলত ৩টি কারণে ব্যবহার করা হয়:

### ১. ফোল্ডার গুছিয়ে রাখা (Organization)

প্রোজেক্ট বড় হলে `app/` ফোল্ডারের ভেতরে অনেকগুলো ফোল্ডার হয়ে যায়। সেগুলোকে লজিক্যাল ক্যাটাগরিতে ভাগ করার জন্য এটি সেরা। যেমন: সব অথেনটিকেশন পেজ এক জায়গায় রাখা, সব মার্কেটিং পেজ এক জায়গায় রাখা।

### ২. আলাদা লেআউট তৈরি করা (Multiple Layouts) 👑

এটিই সবচেয়ে বড় কারণ। ধরুন:

- আপনার **Login/Register** পেজে কোনো সাইডবার থাকবে না।
- আপনার **Dashboard** পেজে একটি বড় সাইডবার থাকবে।

আপনি যদি Route Group ব্যবহার করেন, তবে প্রতিটি গ্রুপের জন্য আলাদা `layout.tsx` ফাইল রাখতে পারবেন।

**📁 উদাহরণ:**

- `app/(auth)/layout.tsx` → এটি শুধু লগইন ও রেজিস্টার পেজে কাজ করবে।
- `app/(dashboard)/layout.tsx` → এটি শুধু সেটিংস বা প্রোফাইল পেজে কাজ করবে।

### ৩. রুট লেআউটকে ভাগ করা (Root Layout Separation)

কখনো কখনো আপনার অ্যাপে এমন পেজ থাকে যেগুলোর ডিজাইন একদম আলাদা (যেমন: একটি ল্যান্ডিং পেজ আর একটি অ্যাডমিন প্যানেল)। তখন আপনি একাধিক `(group)` বানিয়ে প্রতিটিতে আলাদা আলাদা `RootLayout` ব্যবহার করতে পারেন।

---

## ⚙️ Route Groups-এর বৈশিষ্ট্য

| বৈশিষ্ট্য         | বিবরণ                                                                    |
| ----------------- | ------------------------------------------------------------------------ |
| **URL impact**    | URL-এ কোনো পরিবর্তন আনে না।                                              |
| **Layouts**       | প্রতিটি গ্রুপের নিজস্ব `layout.js`, `loading.js`, `error.js` থাকতে পারে। |
| **Naming**        | ফোল্ডার নামের দুই পাশে `( )` থাকতে হবে।                                  |
| **Nested Groups** | একটি গ্রুপের ভেতরে আরেকটি গ্রুপ রাখা সম্ভব।                              |

---

## 🚫 কিছু গুরুত্বপূর্ণ নিয়ম ও সতর্কতা

1. **কনফ্লিক্ট (Conflict):** আপনি যদি `(marketing)/about/page.tsx` এবং `(shop)/about/page.tsx` তৈরি করেন, তবে Next.js এরর দিবে। কারণ দুটোরই URL হয়ে যাচ্ছে `/about`। অর্থাৎ URL যেন ইউনিক হয়।
2. **Top-level Layout:** আপনি যদি একাধিক রুট লেআউট ব্যবহার করেন, তবে মনে রাখবেন এক গ্রুপ থেকে অন্য গ্রুপে যাওয়ার সময় ফুল পেজ রিফ্রেশ হতে পারে।
3. **ব্র্যাকেট জরুরি:** ফোল্ডার নামের দুই পাশে ব্র্যাকেট না দিলে সেটি সাধারণ রাউট হিসেবে কাজ করবে।

---

## 🔑 এক লাইনে সারাংশ

**Route Groups** হলো URL-কে ক্লিন রেখে ফোল্ডারগুলোকে লজিক্যালি সাজানোর এবং আলাদা আলাদা **Layout** ব্যবহার করার একটি সহজ উপায়।
