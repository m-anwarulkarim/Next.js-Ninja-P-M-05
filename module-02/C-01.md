## 🚀 suppressHydrationWarning

Next.js বা React অ্যাপে কাজ করার সময় আমরা প্রায়ই কনসোলে লাল রঙের Hydration Warning দেখি। এটি কেন হয় এবং `suppressHydrationWarning` কেন দরকার, তা নিচে সহজভাবে আলোচনা করা হলো।

### ১. Hydration কী? (সহজ ভাষায়)

Next.js প্রথমে সার্ভারে HTML তৈরি করে ব্রাউজারে পাঠায় (SSR)। কিন্তু সেই HTML থাকে "মৃত" বা প্রাণহীন (মানে বাটন ক্লিক করলে কাজ করে না)।

- **ধাপ ১:** সার্ভার HTML পাঠায়।
- **ধাপ ২:** ব্রাউজারে জাভাস্ক্রিপ্ট লোড হয়।
- **ধাপ ৩:** React ঐ HTML-এর দায়িত্ব বুঝে নেয় এবং সেটিকে ইন্টারঅ্যাক্টিভ করে তোলে।
  👉 এই প্রক্রিয়াটিকেই বলে **Hydration**।

> **শর্ত:** সার্ভার যে HTML পাঠিয়েছে, আর ক্লায়েন্টে React প্রথমবার যে HTML তৈরি করল—দুটো হুবহু এক হতে হবে।

---

### ২. কেন Hydration Warning আসে?

যখন **Server HTML ≠ Client HTML** হয়, তখন React কনসোলে ওয়ার্নিং দেয়। এটি সাধারণত ঘটে যখন ডাটা ডাইনামিক হয়:

- **সময় ও তারিখ:** `new Date()` সার্ভারে এক রকম, ইউজারের পিসিতে আরেক রকম।
- **র‍্যান্ডম নম্বর:** `Math.random()` দুই জায়গায় দুই রকম ভ্যালু দেয়।
- **ব্রাউজার API:** `window` বা `localStorage` যা সার্ভারে নেই, শুধু ক্লায়েন্টে আছে।
- **থিম (Theme):** ইউজার ডার্ক মোড না লাইট মোড ব্যবহার করছে, তা সার্ভার আগে থেকে জানে না।

---

### ৩. suppressHydrationWarning কী?

এটি React-এর একটি স্পেশাল প্রপ (Prop)। এটি React-কে বলে:

> _"বন্ধু, আমি জানি এখানে সার্ভার আর ক্লায়েন্টের ডাটা মিলবে না। তুমি এটা নিয়ে চিল্লাচিল্লি (Warning) করো না, এটাকে ইগনোর করো।"_

**মনে রাখবেন:** এটি সমস্যা সমাধান করে না, শুধু ওয়ার্নিংটাকে লুকিয়ে ফেলে।

---

### ৪. বাস্তব উদাহরণ ও কোড

#### ❌ সমস্যা (ওয়ার্নিং আসবে):

```jsx
<p>বর্তমান সময়: {new Date().toLocaleTimeString()}</p>
```

এখানে সার্ভারের ঘড়ি আর ইউজারের ঘড়ি মিলবে না, তাই ওয়ার্নিং আসবে।

#### ✅ সমাধান (suppressHydrationWarning দিয়ে):

```jsx
<p suppressHydrationWarning>বর্তমান সময়: {new Date().toLocaleTimeString()}</p>
```

এখন React আর কোনো ওয়ার্নিং দেবে না।

#### 🌙 ডার্ক/লাইট থিম (সবচেয়ে কমন কেস):

Next.js-এ থিম ম্যানেজ করার সময় `<html>` ট্যাগে এটি ব্যবহার করা জরুরি:

```jsx
<html lang="en" suppressHydrationWarning>
  <body className={theme}>{children}</body>
</html>
```

---

### ৫. কখন ব্যবহার করবেন আর কখন করবেন না?

| ক্ষেত্র           | ব্যবহার করা যাবে? | কেন?                                                 |
| ----------------- | ----------------- | ---------------------------------------------------- |
| **Date / Time**   | ✅ হ্যাঁ          | সময় প্রতি সেকেন্ডে পাল্টায়, তাই mismatch স্বাভাবিক।  |
| **Random Number** | ✅ হ্যাঁ          | সার্ভার ও ক্লায়েন্টে এক হওয়া প্রায় অসম্ভব।           |
| **Theme Class**   | ✅ হ্যাঁ          | লোকাল স্টোরেজ থেকে থিম আসতে দেরি হয়।                 |
| **User Data**     | ❌ না             | ইউজারের নাম বা ব্যালেন্স ভুল দেখালে বড় বাগ হতে পারে। |

---

### 🧠 বেস্ট প্র্যাকটিস (সবচেয়ে নিরাপদ উপায়)

ওয়ার্নিং হাইড না করে সমস্যাটি সমাধান করাই বুদ্ধিমানের কাজ। এর জন্য **useEffect** ব্যবহার করা হয়:

```jsx
const [isMounted, setIsMounted] = useState(false);

useEffect(() => {
  setIsMounted(true); // শুধুমাত্র ব্রাউজারে আসার পর এটি true হবে
}, []);

if (!isMounted) return null; // সার্ভার রেন্ডারের সময় কিছু দেখাবে না

return <div>{window.innerWidth}</div>; // এখন এটি নিরাপদ
```

প্রোজেক্টে কাজ করার সময় শুধু একটা কথা মাথায় রাখবেন— **`suppressHydrationWarning`** কিন্তু কেবল **এক লেভেল গভীর (one level deep)** কাজ করে। অর্থাৎ, আপনি যদি একটি `<div>`-এ এই প্রপটি দেন, তবে শুধু ওই `<div>`-এর টেক্সট বা অ্যাট্রিবিউট নিয়ে রিয়্যাক্ট চুপ থাকবে, তার ভেতরে থাকা অন্য কোনো চাইল্ড কম্পোনেন্টের বড় কোনো অমিল সে ইগনোর করবে না।

ভবিষ্যতে যখন আপনি বড় কোনো প্রোজেক্টে **Dark Mode** বা **Multilingual (i18n)** সাপোর্ট যোগ করবেন, তখন এই কনসেপ্টটি আপনার অনেক সময় বাঁচিয়ে দেবে।

---

**সারসংক্ষেপ:**
`suppressHydrationWarning` হলো একটি "পর্দা" যা ছোটখাটো অমিল ঢেকে রাখে। ছোটখাটো বিষয়ে এটি ব্যবহার করা ঠিক আছে, কিন্তু বড় লজিক বা গুরুত্বপূর্ণ ডাটার ক্ষেত্রে `useEffect` ব্যবহার করে ক্লায়েন্ট-সাইড রেন্ডারিং নিশ্চিত করাই সেরা উপায়।
