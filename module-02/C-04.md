# 🧱 Static vs Dynamic Rendering: মূল ধারণা

Next.js-এ একটি পেজ ইউজারের কাছে কীভাবে পৌঁছাবে, তা নির্ভর করে সেটি স্ট্যাটিক নাকি ডাইনামিক তার ওপর।

### ⏱️ এক নজরে পার্থক্য

| বৈশিষ্ট্য        | Static (SSG)               | Dynamic (SSR)                      |
| ---------------- | -------------------------- | ---------------------------------- |
| **কখন তৈরি হয়?** | `next build` করার সময়      | প্রতিবার Request আসার সময়          |
| **HTML ফাইল**    | একবার তৈরি হয় এবং সেভ থাকে | প্রতিবার নতুন করে তৈরি হয়          |
| **সবার জন্য**    | সবাই একই কন্টেন্ট পায়      | ইউজার ভেদে আলাদা কন্টেন্ট হতে পারে |

---

## 📦 ১. Static Rendering (SSG) — বিস্তারিত

**কীভাবে কাজ করে?** বিল্ড টাইমেই (যখন আপনি প্রজেক্ট ডেপ্লয় করেন) Next.js ডাটা ফেচ করে একটি স্ট্যাটিক HTML ফাইল বানিয়ে ফেলে। এটি CDN (Content Delivery Network) থেকে সার্ভ করা হয়।

### ✅ বৈশিষ্ট্য:

- **✔️ সুপার ফাস্ট:** কারণ ব্রাউজারে সরাসরি রেডিমেড HTML চলে আসে।
- **✔️ SEO Friendly:** সার্চ ইঞ্জিন খুব সহজে ডাটা রিড করতে পারে।
- **✔️ কম খরচ:** সার্ভারের ওপর চাপ কম পড়ে।

### 🧪 উদাহরণ (Static Example):

```tsx
// app/blog/page.tsx
export default async function Page() {
  // Next.js ডিফল্টভাবে এটাকে ক্যাশ করে রাখবে
  const posts = await fetch("https://api.example.com/posts");
  const data = await posts.json();

  return <PostList posts={data} />;
}
```

👉 **Default Behaviour:** Next.js নিজে থেকেই ধরে নেয় ডাটা ঘনঘন পাল্টাবে না, তাই সে এটিকে স্ট্যাটিক বানিয়ে ফেলে।

---

## 🔴 ২. Dynamic Rendering (SSR) — বিস্তারিত

**কীভাবে কাজ করে?** যখনই কোনো ইউজার পেজটি ভিজিট করে, সার্ভার তখনই ডাটা ফেচ করে এবং নতুন একটি HTML ফাইল তৈরি করে ইউজারের কাছে পাঠায়।

### ✅ বৈশিষ্ট্য:

- **✔️ পার্সোনালাইজড:** ইউজারের নাম, প্রোফাইল বা ড্যাশবোর্ড দেখানোর জন্য সেরা।
- **✔️ রিয়েল-টাইম:** সবসময় লেটেস্ট ডাটা দেখায়।
- **❌ কিছুটা স্লো:** কারণ প্রতিবার সার্ভারে কাজ করতে হয়।

### 🧪 উদাহরণ (Dynamic Example):

```tsx
import { cookies } from "next/headers";

export default async function Page() {
  const cookieStore = cookies(); // কুকি ব্যবহার করলেই এটি ডাইনামিক হয়ে যাবে
  const token = cookieStore.get("token");

  return <p>Your token is: {token?.value}</p>;
}
```

👉 **Force Dynamic:** `cookies()` বা `headers()` ব্যবহার করলে Next.js বুঝে নেয় এই পেজটি স্ট্যাটিক রাখা সম্ভব নয়।

---

## ⚙️ ৩. Static ↔ Dynamic কন্ট্রোল করার উপায়

আপনি চাইলে নিচের ৩টি পদ্ধতিতে কন্ট্রোল করতে পারেন:

### ১. Fetch Cache Option (সবচেয়ে গুরুত্বপূর্ণ)

- **Static:** `fetch(url, { cache: "force-cache" })` (এটি ডিফল্ট)
- **Dynamic:** `fetch(url, { cache: "no-store" })` (প্রতিবার নতুন ডাটা আনবে)

### ২. Route-level Config

আপনি যদি পুরো ফাইলকে স্ট্যাটিক বা ডাইনামিক করতে চান:

```tsx
export const dynamic = "force-static"; // স্ট্যাটিক করার জন্য
export const dynamic = "force-dynamic"; // ডাইনামিক করার জন্য
```

### ৩. Revalidation (ISR) — সেরা সমাধান

এটি স্ট্যাটিক এবং ডাইনামিকের একটি মিশ্রণ। ডাটা নির্দিষ্ট সময় পর পর আপডেট হবে।

```tsx
export const revalidate = 60; // ৬০ সেকেন্ড পর পর ব্যাকগ্রাউন্ডে আপডেট হবে
```

---

## 🧠 ৪. প্রায়োরিটি রুল (Must Remember)

> **Dynamic things > Static things**

যদি আপনার কোনো পেজে ১০টি স্ট্যাটিক ডাটা থাকে এবং মাত্র ১টি ডাইনামিক ডাটা (যেমন: কুকি বা `no-store` ফেচ) থাকে, তবে পুরো পেজটিই **Dynamic** হয়ে যাবে।

---

## 🌍 ৫. কোন ক্ষেত্রে কোনটি ব্যবহার করবেন?

| Use Case                         | Best Strategy               |
| -------------------------------- | --------------------------- |
| **ব্লগ বা নিউজ পোর্টাল**         | Static + ISR (`revalidate`) |
| **ল্যান্ডিং পেজ বা পোর্টফোলিও**  | Static                      |
| **ইউজার ড্যাশবোর্ড বা সেটিংস**   | Dynamic                     |
| **সার্চ রেজাল্ট পেজ**            | Dynamic (`searchParams`)    |
| **প্রোডাক্ট লিস্ট (E-commerce)** | Static + ISR                |
| **কার্ট বা চেকআউট পেজ**          | Dynamic                     |

---

### 🔥 সারাংশ (SEO & Speed)

- **SEO:** দুটোর জন্যই ভালো, তবে স্ট্যাটিক পেজ ক্রলারদের জন্য দ্রুত কাজ করে।
- **Speed:** স্ট্যাটিক অনেক বেশি ফাস্ট।
- **Freshness:** ডাইনামিক সবসময় টাটকা ডাটা দেয়।

<!-- ============================================================ -->

---

# 🏗️ next build চলার সময় কী হয়?

যখন আপনি আপনার টার্মিনালে `npm run build` বা `next build` কমান্ডটি দেন, তখন Next.js আপনার পুরো প্রোজেক্টকে প্রোডাকশনের জন্য প্রস্তুত করতে **Analyze + Pre-render** করে। এই সময়েই মূলত সিদ্ধান্ত নেওয়া হয় কোন পেজটি স্ট্যাটিক হবে আর কোনটি ডাইনামিক।

---

## 🔵 ১. Build Time-এ Static Page এর সাথে কী হয়?

যদি আপনার কোড এমন হয়:

```tsx
// app/blog/page.tsx
export default async function Page() {
  const posts = await fetch("https://api.example.com/posts");
  return <PostList posts={posts} />;
}
```

### **Build সময় Next.js যা করে:**

1. **Code Scan:** পুরো ফাইলটি স্ক্যান করে দেখে।
2. **Check Constraints:** দেখে যে এখানে `cookies()` বা `headers()` এর মতো কোনো ব্রাউজার-নির্ভর জিনিস নেই।
3. **Cache Analysis:** দেখে যে `fetch` ডিফল্ট ক্যাশে আছে।
4. **Decision:** সিদ্ধান্ত নেয় এটি একটি **STATIC** পেজ।

### **📦 ফলাফল:**

- API কল করে ডাটা নিয়ে আসে।
- HTML ফাইল তৈরি করে।
- JSON ডাটা (Payload) তৈরি করে।
- ফাইলগুলো `.next/server/app/blog/page.html` ফোল্ডারে সেভ করে রাখে।

👉 **উৎপাদন (Production) পর্যায়ে:** ইউজার যখনই রিকোয়েস্ট করবে, সার্ভার আর কোড রান করবে না; সরাসরি জেনারেট করা HTML ফাইলটি CDN থেকে পাঠিয়ে দেবে। এটি সুপার ফাস্ট!

---

## 🔴 ২. Build Time-এ Dynamic Page এর সাথে কী হয়?

যদি আপনার কোড এমন হয়:

```tsx
import { cookies } from "next/headers";

export default function Page() {
  cookies(); // ডাইনামিক ফাংশন ব্যবহার
  return <p>Hi</p>;
}
```

### **Build সময়:**

1. **Code Scan:** কোড চেক করে দেখে।
2. **Detection:** দেখে যে এখানে `cookies()` বা `searchParams` ব্যবহার করা হয়েছে ❌।
3. **Decision:** সিদ্ধান্ত নেয় এটি একটি **DYNAMIC** পেজ।

### **📦 ফলাফল:**

- বিল্ড টাইমে কোনো HTML ফাইল জেনারেট করে না।
- শুধু একটি **Route Manifest** বানায় (একটি ম্যাপ যা সার্ভারকে বলে দেয় এই পেজটি ডাইনামিক)।
- `.next` ফোল্ডারে শুধু জাভাস্ক্রিপ্ট কোডটি থাকে যা সার্ভারে রান করবে।

👉 **উৎপাদন (Production) পর্যায়ে:** প্রতিবার রিকোয়েস্ট আসলে সার্ভার কোড রান করবে এবং নতুন HTML তৈরি করবে।

---

## 🔁 ৩. ISR (revalidate) থাকলে Build-এ কী হয়?

```tsx
export const revalidate = 60;
```

এটি হচ্ছে স্ট্যাটিক এবং ডাইনামিকের মাঝামাঝি অবস্থা।

- **Build সময়:** একবার HTML বানিয়ে ক্যাশ করে রাখে।
- **Production এ:** ৬০ সেকেন্ড পর্যন্ত পুরোনো HTML দেখাবে। ৬০ সেকেন্ড পর কোনো রিকোয়েস্ট আসলে ব্যাকগ্রাউন্ডে ডাটা আপডেট করে নতুন HTML জেনারেট করবে এবং পুরোনোটিকে সাইলেন্টলি রিপ্লেস করে দেবে।

---

## ⚙️ Build Time Decision Table

বিল্ডের সময় Next.js নিচের টেবিল অনুযায়ী সিদ্ধান্ত নেয়:

| কন্ডিশন (Condition)                      | বিল্ড ডিসিশন (Build Decision) |
| ---------------------------------------- | ----------------------------- |
| No cookies / No headers                  | **Static**                    |
| fetch (default cache)                    | **Static**                    |
| fetch (no-store)                         | **Dynamic**                   |
| cookies() / headers() ব্যবহৃত            | **Dynamic**                   |
| searchParams (in page)                   | **Dynamic**                   |
| `export const dynamic = "force-dynamic"` | **Dynamic**                   |
| `export const dynamic = "force-static"`  | **Static**                    |

---

### 🧠 মূল সারসংক্ষেপ:

`next build` আসলে একটি **স্মার্ট ফিল্টার**। সে চেষ্টা করে যত বেশি সম্ভব পেজকে স্ট্যাটিক (`.html`) বানিয়ে ফেলতে, যাতে আপনার ওয়েবসাইট রকেটের গতিতে চলে। যেখানে কোনো উপায় থাকে না (যেমন পার্সোনালাইজড ডাটা), শুধু সেখানেই সে ডাইনামিক রেন্ডারিং ব্যবহার করে।
