## 🚀 Parallel Routes: বিস্তারিত গাইড

**Parallel Routes** হলো নেক্সট জেএস-এর এমন একটি শক্তিশালী ফিচার যা আপনাকে একই লেআউটের মধ্যে একাধিক পেজকে (যাদের আমরা **Slots** বলি) একসাথে রেন্ডার করার অনুমতি দেয়। এটি মূলত জটিল ড্যাশবোর্ড বা সোশ্যাল মিডিয়া ফিডের মতো ইন্টারফেস তৈরির জন্য ডিজাইন করা হয়েছে।

### ১. কেন এটি ব্যবহার করবেন? (The "Why")

সাধারণত একটি URL-এ একটি মাত্র `page.tsx` রেন্ডার হয়। কিন্তু Parallel Routes ব্যবহার করলে আপনি একটি URL-এই নিচের সুবিধাগুলো পাবেন:

- **Independent Streaming:** ধরুন আপনার ড্যাশবোর্ডের "Analytics" সেকশনটি লোড হতে ৫ সেকেন্ড লাগে, কিন্তু "Sidebar" ১ সেকেন্ডে লোড হয়। Parallel Route ব্যবহার করলে Sidebar লোড হয়ে যাবে, আর Analytics সেকশনটি তার নিজস্ব `loading.tsx` দেখাবে। কেউ কাউকে ব্লক করবে না।
- **Independent Error Handling:** যদি চ্যাট প্যানেলে কোনো এরর হয়, তবে পুরো ড্যাশবোর্ড ক্র্যাশ করবে না। শুধু চ্যাট সেকশনে `error.tsx` দেখাবে।

---

### ২. ফোল্ডার স্ট্রাকচার ও স্লট (Slots)

Parallel Routes তৈরি করতে `@` সিম্বল ব্যবহার করতে হয়। এই ফোল্ডারগুলোকে বলা হয় **"Slots"**। এগুলো কিন্তু URL এর অংশ হয় না। অর্থাৎ `/dashboard/@sidebar` লিখে ব্রাউজারে সার্চ করলে কিছু আসবে না, এটি শুধু `/dashboard` এই পাওয়া যাবে।

**উদাহরণ স্ট্রাকচার:**

```text
app/
├── dashboard/
│   ├── layout.tsx
│   ├── page.tsx
│   ├── @analytics/
│   │   ├── page.tsx
│   │   └── loading.tsx
│   └── @team/
│       └── page.tsx

```

---

### ৩. লেআউটে ব্যবহারের নিয়ম (Implementation)

আপনি যখন `@analytics` এবং `@team` নামে দুটি স্লট বানাবেন, তখন `dashboard/layout.tsx` ফাইলটি স্বয়ংক্রিয়ভাবে সেগুলোকে **Props** হিসেবে গ্রহণ করবে।

```tsx
// app/dashboard/layout.tsx

export default function DashboardLayout({
  children,
  analytics,
  team,
}: {
  children: React.ReactNode;
  analytics: React.ReactNode;
  team: React.ReactNode;
}) {
  return (
    <div className="flex flex-col gap-4">
      <nav>Navbar</nav>
      <div className="flex gap-4">
        <section className="border p-4">{analytics}</section>
        <section className="border p-4">{team}</section>
      </div>
      <main>{children}</main> {/* এটি dashboard/page.tsx কে রেন্ডার করবে */}
    </div>
  );
}
```

---

### ৪. Default.tsx এর গুরুত্ব (খুবই জরুরি)

Next.js যখন কোনো কারণে একটি স্লটের বর্তমান স্টেট খুঁজে পায় না (যেমন: হার্ড রিফ্রেশ দিলে), তখন সে `default.tsx` ফাইলটি খোঁজে। যদি এই ফাইলটি না থাকে, তবে আপনার পুরো পেজে **404 Error** দেখাতে পারে।

- **কেন দরকার?** যখন আপনি একটি স্লটে নেভিগেট করবেন কিন্তু অন্য স্লটটি আগের জায়গাতেই থাকবে, তখন Next.js কে বলে দিতে হয় যে অন্য স্লটটিতে এখন কী দেখাবে।

---

### ৫. কন্ডিশনাল রেন্ডারিং (Conditional Routes)

Parallel Routes এর সবচেয়ে বড় সুবিধা হলো আপনি ইউজারের রোলের ওপর ভিত্তি করে UI পাল্টে দিতে পারেন।

**উদাহরণ:**

```tsx
export default function Layout({
  dashboard,
  adminPanel,
}: {
  dashboard: React.ReactNode;
  adminPanel: React.ReactNode;
}) {
  const isAdmin = checkUserRole(); // আপনার নিজস্ব লজিক
  return isAdmin ? adminPanel : dashboard;
}
```

---

### 💡 ভালো একটি বাস্তব উদাহরণ: Modals

ধরুন আপনি একটি ছবির গ্যালারি বানাচ্ছেন। ইউজার যখন কোনো ছবিতে ক্লিক করবে, তখন একটি **Modal** ওপেন হবে এবং URL হবে `/photo/1`। কিন্তু ইউজার ব্যাকগ্রাউন্ডে গ্যালারি পেজটিও দেখতে পাবে।

১. `@modal` নামে একটি স্লট থাকবে।
২. গ্যালারি পেজটি হবে `children`।
৩. যখন ইউজার `/photo/1` এ যাবে, তখন `@modal` স্লটে ছবির ডিটেইলস দেখাবে, আর গ্যালারি পেজটি নিচে থেকে যাবে।

---

### 📝 মনে রাখার মতো পয়েন্ট (Summary Table)

| বৈশিষ্ট্য     | বিবরণ                                                                |
| ------------- | -------------------------------------------------------------------- |
| **সিম্বল**    | ফোল্ডারের আগে `@` দিতে হয় (যেমন: `@user`)                            |
| **URL**       | স্লটের নাম URL-এ দেখা যায় না।                                        |
| **Props**     | Layout ফাইলে স্লটের নাম অনুযায়ী Prop হিসেবে আসে।                     |
| **Streaming** | প্রতিটি স্লট আলাদাভাবে লোড হতে পারে (Fast UX)।                       |
| **Fallback**  | হার্ড রিফ্রেশ হ্যান্ডেল করার জন্য `default.tsx` ব্যবহার বাধ্যতামূলক। |

---

### ❌ কমন ভুল যা এড়িয়ে চলবেন:

- **স্লটকে URL ভাবা:** `@sidebar` মানে এই না যে আপনাকে `/dashboard/@sidebar` এ যেতে হবে। এটি শুধু লেআউটের একটি অংশ।
- **Default ফাইল না রাখা:** বড় প্রজেক্টে `default.tsx` না রাখলে নেভিগেশনের সময় অদ্ভুত বাগ দেখা দিতে পারে।

<!-- ============================================================================= -->

নিচে একটি গ্যালারি অ্যাপের উদাহরণ দেওয়া হলো যেখানে কোনো ছবিতে ক্লিক করলে একটি মডাল ওপেন হবে।

### ১. ফোল্ডার স্ট্রাকচার

প্রথমে আমাদের স্লট হিসেবে `@modal` ফোল্ডারটি তৈরি করতে হবে।

```text
app/
├── layout.tsx         (মেইন লেআউট যেখানে স্লট রেন্ডার হবে)
├── page.tsx           (গ্যালারি পেজ)
├── @modal/
│   ├── default.tsx    (মডাল যখন বন্ধ থাকবে তখন কী দেখাবে)
│   └── [id]/
│       └── page.tsx   (মডালের ভেতরের কন্টেন্ট)
└── photo/[id]/
    └── page.tsx       (সরাসরি লিঙ্কে ঢুকলে যে পেজ দেখাবে - Fallback)

```

---

### ২. মেইন লেআউট সেটআপ

এখানে আমরা `children` (গ্যালারি) এবং `modal` (স্লট) দুটোই রেন্ডার করবো।

```tsx
// app/layout.tsx
export default function RootLayout({
  children,
  modal,
}: {
  children: React.ReactNode;
  modal: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <main>{children}</main>
        {modal} {/* এখানে মডালটি রেন্ডার হবে */}
      </body>
    </html>
  );
}
```

---

### ৩. গ্যালারি পেজ (Trigger)

এখান থেকে ইউজার ফটোতে ক্লিক করবে।

```tsx
// app/page.tsx
import Link from "next/link";

export default function Gallery() {
  const photos = [1, 2, 3];
  return (
    <div className="grid grid-cols-3 gap-4 p-10">
      {photos.map((id) => (
        <Link key={id} href={`/photo/${id}`} scroll={false}>
          <div className="bg-gray-200 p-20 cursor-pointer">Photo {id}</div>
        </Link>
      ))}
    </div>
  );
}
```

---

### ৪. মডাল কন্টেন্ট তৈরি

ইউজার যখন ক্লিক করবে, তখন `@modal/[id]/page.tsx` রেন্ডার হবে।

```tsx
// app/@modal/[id]/page.tsx
import { useRouter } from "next/navigation";

export default function PhotoModal({ params }: { params: { id: string } }) {
  return (
    <div className="fixed inset-0 bg-black/50 flex items-center justify-center">
      <div className="bg-white p-10 rounded-lg relative">
        <h2>Viewing Photo: {params.id}</h2>
        <p>এটি একটি Parallel Route মডাল!</p>
        <button className="mt-4 bg-blue-500 text-white p-2">Close</button>
      </div>
    </div>
  );
}
```

---

### ৫. Default.tsx কেন লাগবে?

যখন মডাল ওপেন থাকবে না বা ইউজার যখন হোম পেজে থাকবে, তখন `@modal` স্লটে কিছুই দেখানোর দরকার নেই। তাই একটি `default.tsx` ফাইল রাখতে হয় যা `null` রিটার্ন করবে।

```tsx
// app/@modal/default.tsx
export default function Default() {
  return null;
}
```

---

### এই উদাহরণের বিশেষত্ব কী?

১. **URL চেঞ্জ:** ছবিতে ক্লিক করলে URL হবে `/photo/1`, কিন্তু পেজ রিলোড হবে না, শুধু মডাল আসবে।
২. **Deep Linking:** যদি কেউ সরাসরি `your-site.com/photo/1` এ যায়, তবে সে আলাদা একটি ডেডিকেটেড ফটো পেজ দেখবে (মডাল নয়), কারণ তখন Next.js `app/photo/[id]/page.tsx` খুঁজে নিবে।
৩. **UX:** ইউজার ব্যাক বাটনে ক্লিক করলে মডাল বন্ধ হয়ে যাবে এবং আগের গ্যালারি পেজেই থাকবে।

<!-- ================================================= -->

Parallel Routes ব্যবহারের সময় কিছু জটিল টেকনিক্যাল চ্যালেঞ্জ বা সমস্যার সম্মুখীন হতে হয়। এগুলো না জানলে অ্যাপ্লিকেশনে অদ্ভুত সব এরর (Bugs) দেখা দিতে পারে। নিচে বিস্তারিত সমস্যাগুলো তুলে ধরা হলো:

---

### ১. `default.tsx` ফাইল না থাকার সমস্যা (The 404 Error)

এটি Parallel Route-এর সবচেয়ে কমন সমস্যা। Next.js যখন বুঝতে পারে না যে একটি নির্দিষ্ট স্লটে (Slot) বর্তমানে কী রেন্ডার করা উচিত, তখন সে `default.tsx` ফাইলটি খোঁজে।

- **সমস্যা:** আপনি যদি কোনো স্লটের জন্য `default.tsx` ফাইল না রাখেন, তবে হার্ড রিফ্রেশ (F5) দিলে বা সরাসরি ওই URL-এ প্রবেশ করলে পুরো পেজটি **404 Not Found** দেখাবে।
- **কেন হয়:** সফট নেভিগেশনের সময় Next.js আগের স্টেট মনে রাখে, কিন্তু হার্ড রিফ্রেশ দিলে সে আগের স্লটের কন্টেন্ট খুঁজে পায় না।

### ২. Navigation-এর বিভ্রান্তি (State Mismatch)

ধরা যাক আপনার ড্যাশবোর্ডে দুটি স্লট আছে: `@analytics` এবং `@team`।

- **সমস্যা:** আপনি যদি শুধু `@analytics` স্লটের ভেতরের কোনো লিঙ্কে ক্লিক করে নেভিগেট করেন, তাহলে `@team` স্লটটিতে কী দেখাবে?
- **ফলাফল:** যদি আপনি `@team` এর জন্য নতুন কোনো পেজ না তৈরি করে থাকেন, তবে ব্রাউজার রিফ্রেশ দিলে `@team` অংশটি হারিয়ে যাবে যদি সেখানে `default.tsx` না থাকে। এটি ইউজার এক্সপেরিয়েন্স নষ্ট করে।

### ৩. Client-side State হারানো

Parallel Routes সাধারণত একই লেআউটে একাধিক পেজ রেন্ডার করে।

- **সমস্যা:** আপনি যদি এক স্লট থেকে অন্য স্লটে ডেটা পাস করতে চান, তবে এটি সাধারণ Props এর মাধ্যমে সম্ভব নয়। কারণ তারা ভাই-বোন (Siblings) হিসেবে থাকে।
- **সমাধান:** এক্ষেত্রে আপনাকে **Context API** বা **Zustand/Redux** এর মতো স্টেট ম্যানেজমেন্ট টুল ব্যবহার করতে হবে।

### ৪. Modals এবং Refresh-এর সমস্যা (Intercepting Routes)

আপনি যখন মডাল হিসেবে Parallel Route ব্যবহার করেন (যেমনটি আগের উদাহরণে দেখেছি):

- **সমস্যা:** ইউজার যখন মডাল ওপেন থাকা অবস্থায় পেজ রিফ্রেশ দেয়, তখন আর মডালটি থাকে না। তখন সে সরাসরি মূল পেজটিতে (`app/photo/[id]/page.tsx`) চলে যায়।
- **কেন সমস্যা:** অনেক সময় ডেভেলপাররা চান রিফ্রেশ দিলেও মডালটি থাকুক, কিন্তু Parallel Route একা এটি হ্যান্ডেল করতে পারে না। এর জন্য **Intercepting Routes** (`(..)folderName`) ব্যবহার করতে হয়।

### ৫. Static Site Generation (SSG) এ সীমাবদ্ধতা

- **সমস্যা:** যদি আপনার কোনো একটি স্লট Dynamic হয় (যেমন: ইউজারের ডেটা দেখায়), তবে পুরো পেজটিকেই Dynamic রেন্ডারিংয়ে পাঠাতে হতে পারে। এর ফলে স্ট্যাটিক জেনারেশনের সুবিধা সব সময় পুরোপুরি নেওয়া যায় না।

### ৬. কমপ্লেক্স লেআউট ম্যানেজমেন্ট

- **সমস্যা:** যখন আপনার অনেকগুলো স্লট হয়ে যায় (যেমন: `@sidebar`, `@header`, `@footer`, `@feed`, `@ads`), তখন `layout.tsx` ফাইলটি অনেক বড় এবং জটিল হয়ে যায়। প্রতিটা স্লটের জন্য আলাদা আলাদা `loading.tsx` এবং `error.tsx` ম্যানেজ করা ডেভেলপারদের জন্য কষ্টসাধ্য হতে পারে।

---

### 💡 এই সমস্যাগুলো সমাধানের সহজ উপায়:

| সমস্যা                  | সমাধান                                                                                     |
| ----------------------- | ------------------------------------------------------------------------------------------ |
| **404 Error**           | সব সময় প্রতিটা স্লট ফোল্ডারে একটি `default.tsx` ফাইল রাখুন।                                |
| **Data Sharing**        | স্লটগুলোর মধ্যে ডেটা শেয়ার করতে Context API ব্যবহার করুন।                                  |
| **Modal Sync**          | Parallel Route-এর সাথে Intercepting Routes ব্যবহার করুন।                                   |
| **Independent Loading** | প্রতিটি স্লটের ভেতরে আলাদা `loading.tsx` ব্যবহার করুন যাতে একটির জন্য অন্যটি আটকে না থাকে। |
