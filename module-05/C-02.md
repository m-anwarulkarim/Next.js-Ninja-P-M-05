# 🚀 Next.js App Router এ `generateStaticParams`

## 🔹 কী এটা?

`generateStaticParams` হলো **Next.js App Router (v13+)** এর একটি function  
👉 কাজ: **Dynamic route গুলোকে build time-এ static HTML বানানো।**

**Formula:**  
`Dynamic Route + SSG (Static Site Generation)` = `generateStaticParams`

---

## 🔹 কোথায় ব্যবহার হয়?

শুধু **dynamic route** এর জন্য 👇

```txt
app/blog/[id]/page.tsx
app/products/[slug]/page.tsx
```

---

## 🔹 কেন দরকার?

আগে Next.js জানত না build time-এ কোন কোন dynamic page বানাতে হবে।  
যেমন:

```
/blog/1
/blog/2
/blog/3
```

👉 তাই Next.js কে আগেই বলে দিতে হয়:  
**“এই এই params দিয়ে page বানাও”**  
এটাই করে 👉 `generateStaticParams`

---

## 🧩 Basic Example

```ts
// app/blog/[id]/page.tsx
export async function generateStaticParams() {
  return [
    { id: "1" },
    { id: "2" },
    { id: "3" },
  ];
}

const BlogPage = ({ params }: { params: { id: string } }) => {
  return <div>Blog ID: {params.id}</div>;
};

export default BlogPage;
```

👉 Build time-এ বানাবে:

- `/blog/1.html`
- `/blog/2.html`
- `/blog/3.html`

---

## 🌐 Real-Life Example (API থেকে Data)

ধরো backend এ অনেক blog আছে → সবগুলোকে static বানাতে চাই।

```ts
export async function generateStaticParams() {
  const res = await fetch("https://api.example.com/blogs");
  const blogs = await res.json();

  return blogs.map((blog: any) => ({
    id: blog.id.toString(),
  }));
}
```

👉 Next.js করবে:

- Build time-এ API call
- সব blog page static বানাবে

---

## 🧪 তোমার দেওয়া Example (Top 3 Blogs)

```ts
export async function generateStaticParams() {
  const { data } = await blogService.getAllBlog();

  // শুধু প্রথম ৩টা blog-এর জন্য static বানাচ্ছি
  const res = data.data
    .map((blog: BlogPost) => ({ id: blog.id.toString() }))
    .splice(0, 3);

  return res;
}
```

👉 এখানে Next.js বানাবে শুধু প্রথম ৩টা blog-এর জন্য:

- `/blog/1.html`
- `/blog/2.html`
- `/blog/3.html`

বাকি blog runtime-এ SSR হবে অথবা 404 (যদি `dynamicParams = false` থাকে)।

---

## ⚠️ Important Rules

- ❌ Client Component এ কাজ করে না
- ✅ শুধু Server Component এ কাজ করে
- `next build` এ call হয়, runtime এ না
- `fetch` সবসময় cached থাকে

---

## 🔑 `dynamicParams`

- Default: `true` → extra params runtime এ SSR হবে
- যদি `false` দাও → শুধু static params কাজ করবে, অন্যগুলো 404

---

## 🔄 তুলনা: পুরোনো vs নতুন

| Pages Router (Old) | App Router (New)       |
| ------------------ | ---------------------- |
| `getStaticPaths`   | `generateStaticParams` |
| `fallback`         | `dynamicParams`        |
| `getStaticProps`   | Server Component       |

---

## 🎯 কখন ব্যবহার করবে?

✅ ব্যবহার করো যদি:

- Blog
- Product page
- Documentation
- SEO important page

❌ ব্যবহার করোনা যদি:

- User dashboard
- Auth based page
- Frequently changing data
- Real-time content

---

## 🚫 Common Mistakes

- `params` কে Promise বানানো
- Client Component এ লেখা
- Runtime data expect করা
- Auth required API call

---

## 🧠 One Line Summary

> `generateStaticParams` ব্যবহার করে Next.js কে আগেই বলে দেওয়া হয়  
> **কোন কোন dynamic route build time-এ static বানাতে হবে।**
