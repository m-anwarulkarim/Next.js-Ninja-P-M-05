## 🚀 Next.js Slug (Dynamic Routing) - একদম শুরু থেকে শেষ পর্যন্ত

Next.js-এর **App Router**-এ Slug সম্পর্কিত সব বিষয় নিচে ধাপে ধাপে এবং উদাহরণসহ ব্যাখ্যা করা হলো:

### 1️⃣ Slug কী?

Slug হচ্ছে URL-এর একটি dynamic বা পরিবর্তনশীল অংশ, যা দিয়ে নির্দিষ্ট ডাটা বা পেজকে শনাক্ত করা হয়।

- **উদাহরণ:** `/blog/next-js-tutorial` এখানে `next-js-tutorial` হলো slug।

---

### 2️⃣ Slug কেন ব্যবহার করবেন?

- **Dynamic Page:** হাজার হাজার পেজের জন্য আলাদা ফাইল না বানিয়ে একটি ফাইল দিয়েই সব হ্যান্ডেল করা যায়।
- **SEO Friendly:** `/blog/102` এর চেয়ে `/blog/how-to-learn-nextjs` পড়া সহজ এবং সার্চ ইঞ্জিনের জন্য ভালো।

---

### 3️⃣ App Router-এ Slug তৈরির নিয়ম

ফোল্ডার তৈরির সময় থার্ড ব্র্যাকেট `[]` ব্যবহার করতে হয়।

- 📁 **Structure:** `src/app/blog/[slug]/page.tsx`
- এখানে **[slug]** হলো একটি Dynamic Segment।

---

### 4️⃣ Server Component-এ Slug পাওয়ার উপায়

Next.js ডিফল্টভাবে `params` প্রপস পাঠিয়ে দেয়।

```tsx
interface PageProps {
  params: Promise<{ slug: string }>; // string array
}
export default function BlogDetails({ params }: PageProps) {
  const { slug } = params; // URL থেকে slug টি এখানে আসবে

  return (
    <div>
      <h1>Showing Blog: {slug}</h1>
    </div>
  );
}
```

---

### 5️⃣ Slug দিয়ে API থেকে Data আনা

```tsx
export default async function BlogDetails({ params }: PageProps) {
  const res = await fetch(`https://api.example.com/post/${params.slug}`);
  const data = await res.json();

  return (
    <article>
      <h1>{data.title}</h1>
      <p>{data.content}</p>
    </article>
  );
}
```

---

### 6️⃣ বিভিন্ন ধরণের Slug (Advanced)

| টাইপ                   | ফোল্ডার ফরম্যাট | URL উদাহরণ          | Params রেজাল্ট                  |
| ---------------------- | --------------- | ------------------- | ------------------------------- |
| **Single Slug**        | `[slug]`        | `/blog/react`       | `{ slug: 'react' }`             |
| **Catch-all**          | `[...slug]`     | `/docs/v1/intro`    | `{ slug: ['v1', 'intro'] }`     |
| **Optional Catch-all** | `[[...slug]]`   | `/docs` (বা যেকোনো) | `{ slug: undefined }` বা অ্যারে |

---

### 7️⃣ Client Component-এ Slug (useParams)

যদি পেজটি `"use client"` হয়, তবে `useParams` হুক ব্যবহার করতে হবে।

```tsx
"use client";
import { useParams } from "next/navigation";

export default function ClientPage() {
  const params = useParams();
  return <div>Slug is: {params.slug}</div>;
}
```

---

### 8️⃣ params vs searchParams

- **params:** URL পাথের অংশ (যেমন: `/blog/react`) -> `react`
- **searchParams:** URL-এর পরের কুয়েরি (যেমন: `/blog/react?id=1`) -> `id=1`

---

### 9. কমন ভুলসমূহ (Avoid These ❌)

1. **ভুল:** ফোল্ডারের নাম `[slug].tsx` রাখা। (সঠিক: `[slug]/page.tsx`)
2. **ভুল:** একই ফোল্ডারে `blog/page.tsx` এবং `blog/[slug]/page.tsx` রাখা যদি তারা একে অপরের সাথে কনফ্লিক্ট করে।
3. **ভুল:** Client Component-এ সরাসরি `params` ডিস্ট্রাকচার করা। (সঠিক: `useParams()` ব্যবহার করা)

---

### 🧠 সংক্ষেপে মনে রাখার টিপস:

- **একটা ভ্যালু** ধরতে চাইলে: `[slug]`
- **একাধিক ভ্যালু (অ্যারে)** ধরতে চাইলে: `[...slug]`
- **অপশনাল** রাখতে চাইলে: `[[...slug]]`
- সার্ভারে **params**, ক্লায়েন্টে **useParams()**।
