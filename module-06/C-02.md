# Next.js-এ `params` এবং `searchParams` - সম্পূর্ণ গাইড

Next.js-এ `params` এবং `searchParams` খুবই গুরুত্বপূর্ণ দুটি কনসেপ্ট। এগুলো কোনো এক্সটারনাল লাইব্রেরি নয়, বরং **Next.js-এর বিল্ট-ইন (Built-in)** ফিচার।

---

## ১. Params (Dynamic Route Parameters)

এটি ব্যবহার করা হয় যখন কোনো URL-এর **অংশ** পরিবর্তনশীল বা ডাইনামিক হয়। এটি সাধারণত ফোল্ডার স্ট্রাকচারের ওপর ভিত্তি করে কাজ করে।

### ✅ মূল বৈশিষ্ট্য:

- **URL স্ট্রাকচার:** `/user/123` বা `/blog/my-first-post`
- **ফোল্ডার নাম:** `app/user/[id]/page.js` (এখানে `[id]` হলো params)
- **কাজ:** নির্দিষ্ট কোনো রিসোর্স (যেমন: একজন নির্দিষ্ট ইউজার বা একটি পোস্ট) খুঁজে বের করতে এটি ব্যবহৃত হয়

### উদাহরণ:

URL যদি হয় `mysite.com/product/iphone`, তবে:

```javascript
// params এর মান হবে:
{
  id: "iphone";
}
```

### 📁 Multiple Dynamic Segments:

```
ফোল্ডার: app/shop/[category]/[productId]/page.js
URL: mysite.com/shop/electronics/laptop-123

params = {
  category: "electronics",
  productId: "laptop-123"
}
```

---

## ২. SearchParams (Query Parameters)

এটি ব্যবহার করা হয় যখন URL-এর শেষে প্রশ্নবোধক চিহ্নের (`?`) পরে অতিরিক্ত কিছু তথ্য পাঠানো হয়। একে **Query Strings**-ও বলা হয়।

### ✅ মূল বৈশিষ্ট্য:

- **URL স্ট্রাকচার:** `/shop?category=shoes&color=red`
- **ফোল্ডার নাম:** এর জন্য কোনো স্পেশাল ফোল্ডার লাগে না। সাধারণ `app/shop/page.js`-এই এটি পাওয়া যায়
- **কাজ:** পেজের ডাটা ফিল্টার করা, সর্ট করা বা সার্চ করার জন্য এটি ব্যবহৃত হয়

### উদাহরণ:

URL যদি হয় `mysite.com/shop?sort=price-low&page=2`, তবে:

```javascript
// searchParams এর মান হবে:
{
  sort: "price-low",
  page: "2"
}
```

### 🔍 Multiple Query Parameters:

```
URL: /products?category=laptop&brand=apple&minPrice=1000&maxPrice=3000

searchParams = {
  category: "laptop",
  brand: "apple",
  minPrice: "1000",
  maxPrice: "3000"
}
```

---

## ৩. মূল পার্থক্য টেবিল

| বৈশিষ্ট্য             | Params                                   | SearchParams                     |
| --------------------- | ---------------------------------------- | -------------------------------- |
| **URL ভিউ**           | `/blog/post-1` (স্ল্যাশ দিয়ে থাকে)      | `/blog?page=1` (`?` এর পর থাকে)  |
| **ফোল্ডার নাম**       | ডাইনামিক ফোল্ডার `[id]` লাগে             | কোনো স্পেশাল ফোল্ডার লাগে না     |
| **উদ্দেশ্য**          | নির্দিষ্ট পেজ বা রিসোর্স আইডেন্টিফাই করা | ফিল্টারিং, সার্চিং বা সর্টিং করা |
| **Required/Optional** | সাধারণত Required (পেজ লোডের জন্য লাগে)   | সবসময় Optional                  |
| **SEO Friendly**      | ✅ হ্যাঁ (clean URL)                     | ⚠️ কম (query string visible)     |
| **Next.js ডিফল্ট**    | ✅ হ্যাঁ (Built-in)                      | ✅ হ্যাঁ (Built-in)              |
| **Page/Layout**       | দুটোতেই পাওয়া যায়                      | শুধু Page-এ পাওয়া যায়          |

---

## ৪. কোডে ব্যবহার (Next.js 15 Server Component)

Next.js-এর লেটেস্ট ভার্সনে এই দুটিই **Promise** হিসেবে আসে, তাই **await** করতে হয়।

### 📝 Basic Example:

```javascript
// app/product/[id]/page.js

export default async function ProductPage({ params, searchParams }) {
  // Promise হ্যান্ডেল করার জন্য await করতে হবে
  const { id } = await params;
  const { color, size } = await searchParams;

  return (
    <div>
      <h1>Product ID: {id}</h1> {/* Output: iphone */}
      <p>Selected Color: {color}</p> {/* Output: black */}
      <p>Selected Size: {size}</p> {/* Output: 256gb */}
    </div>
  );
}
```

**URL হবে:** `/product/iphone?color=black&size=256gb`

---

## ৫. Practical Example - Database থেকে Data Fetch

নিচে **`params`** ব্যবহার করে ডাটাবেস (যেমন: Prisma বা API) থেকে নির্দিষ্ট ডাটা ফেচ করার একটি প্র্যাকটিক্যাল উদাহরণ দেওয়া হলো।

ধরে নিন, আপনার একটি ব্লগের জন্য ডাইনামিক পেজ আছে যার ফোল্ডার স্ট্রাকচার হলো: `app/blog/[slug]/page.js`

### Example: Dynamic Blog Post Fetching

```javascript
// app/blog/[slug]/page.js

import { notFound } from "next/navigation";

// ১. এটি একটি সার্ভার কম্পোনেন্ট
export default async function BlogPostPage({ params }) {
  // ২. Next.js 15+ এ params কে await করতে হয়
  const { slug } = await params;

  // ৩. ডাটাবেস থেকে ডাটা ফেচ করা (এখানে একটি ফেক API বা Prisma কল্পনা করুন)
  const post = await fetchPostFromDB(slug);

  // ৪. যদি ডাটা না পাওয়া যায়, তবে 404 পেজে পাঠিয়ে দাও
  if (!post) {
    notFound();
  }

  return (
    <article className="p-10">
      <h1 className="text-3xl font-bold">{post.title}</h1>
      <p className="mt-4 text-gray-600">{post.content}</p>

      <div className="mt-6 text-sm text-blue-500">Post ID from URL: {slug}</div>
    </article>
  );
}

// ডাটা ফেচ করার কাল্পনিক ফাংশন
async function fetchPostFromDB(slug) {
  // এখানে আপনার DB Query বা API Call হবে
  // যেমন: return await prisma.post.findUnique({ where: { slug } })

  const posts = {
    "nextjs-guide": {
      title: "Next.js Learning Guide",
      content: "Next.js is awesome...",
    },
    "react-tips": {
      title: "React Tips & Tricks",
      content: "Always use functional components...",
    },
  };

  return posts[slug] || null;
}
```

### 🔍 এখানে যা যা ঘটছে:

1. **Dynamic Route:** `[slug]` ফোল্ডারটির কারণে URL হবে `yoursite.com/blog/nextjs-guide`। এখানে `nextjs-guide` অংশটিই হলো আমাদের `params` বা `slug`
2. **Server-Side Fetching:** যেহেতু এটি একটি Server Component, আপনি সরাসরি এখানে ডাটাবেস কল করতে পারছেন (যেমন `Prisma` বা `Mongoose` দিয়ে), কোনো ক্লায়েন্ট সাইড `useEffect` লাগছে না
3. **Built-in 404:** যদি ইউজার এমন কোনো লিঙ্কে যায় যা আপনার ডাটাবেসে নেই (যেমন: `/blog/non-existent-post`), তবে Next.js-এর বিল্ট-ইন **`notFound()`** ফাংশন ইউজারকে সরাসরি 404 পেজে নিয়ে যাবে

---

## ৬. Params + SearchParams একসাথে ব্যবহার

এখন দেখি কীভাবে দুটো একসাথে ব্যবহার করে আরো powerful feature তৈরি করা যায়।

### Example: E-commerce Product Page with Filters

```javascript
// app/products/[category]/page.js

export default async function CategoryPage({ params, searchParams }) {
  const { category } = await params;
  const { sort, minPrice, maxPrice, page } = await searchParams;

  // Database থেকে filtered products fetch করা
  const products = await fetchProducts({
    category: category, // params থেকে
    sortBy: sort || "newest", // searchParams থেকে
    minPrice: minPrice || 0, // searchParams থেকে
    maxPrice: maxPrice || 999999, // searchParams থেকে
    page: page || 1, // searchParams থেকে
  });

  return (
    <div>
      <h1>Category: {category}</h1>

      {/* Filter UI */}
      <div className="filters">
        <p>Sort: {sort || "newest"}</p>
        <p>
          Price Range: ${minPrice || 0} - ${maxPrice || "Max"}
        </p>
      </div>

      {/* Products Grid */}
      <div className="grid grid-cols-3 gap-4">
        {products.map((product) => (
          <ProductCard key={product.id} product={product} />
        ))}
      </div>
    </div>
  );
}
```

**URL Example:**

```
/products/electronics?sort=price-low&minPrice=500&maxPrice=2000&page=2
           ↑              ↑
        params       searchParams
```

---

## ৭. Client Component-এ কীভাবে ব্যবহার করবেন?

Server Component-এ `params` আর `searchParams` directly পাওয়া যায়, কিন্তু **Client Component-এ** আলাদা approach লাগে।

### 🔴 Client Component এ searchParams:

```javascript
"use client";

import { useSearchParams, usePathname } from "next/navigation";

export default function ProductFilters() {
  const searchParams = useSearchParams();
  const pathname = usePathname();

  const currentSort = searchParams.get("sort"); // "price-low"
  const currentPage = searchParams.get("page"); // "2"

  return (
    <div>
      <p>Current Sort: {currentSort}</p>
      <p>Current Page: {currentPage}</p>
      <p>Current Path: {pathname}</p>
    </div>
  );
}
```

### 🔴 Client Component এ params:

```javascript
"use client";

import { useParams } from "next/navigation";

export default function ProductDetails() {
  const params = useParams();

  const productId = params.id; // "laptop-123"
  const category = params.category; // "electronics"

  return <div>Product: {productId}</div>;
}
```

---

## ৮. TypeScript Support

Type safety এর জন্য proper typing করুন:

```typescript
// app/blog/[slug]/page.tsx

interface BlogPageProps {
  params: Promise<{ slug: string }>;
  searchParams: Promise<{
    theme?: string;
    page?: string;
    sort?: 'newest' | 'oldest' | 'popular';
  }>;
}

export default async function BlogPostPage({
  params,
  searchParams
}: BlogPageProps) {
  const { slug } = await params;
  const { theme, page, sort } = await searchParams;

  // Type-safe access
  console.log(slug);   // string
  console.log(theme);  // string | undefined
  console.log(page);   // string | undefined
  console.log(sort);   // 'newest' | 'oldest' | 'popular' | undefined

  return <div>...</div>
}
```

---

## ৯. Important Tips & Best Practices

### ✅ করবেন:

- `params` ব্যবহার করুন resource identification এর জন্য
- `searchParams` ব্যবহার করুন filtering, sorting, pagination এর জন্য
- সবসময় `await` করুন Next.js 15+ এ
- TypeScript ব্যবহার করলে proper typing করুন
- `notFound()` ব্যবহার করুন যখন data না পাওয়া যায়

### ❌ করবেন না:

- Sensitive data `searchParams` এ পাঠাবেন না (visible in URL)
- অনেক বড় data `searchParams` এ পাঠাবেন না
- `params` কে optional হিসেবে treat করবেন না (এটা required)

---

## ১০. সারসংক্ষেপ

### 🎯 মনে রাখার মূল পয়েন্ট:

| Feature              | Params                        | SearchParams                |
| -------------------- | ----------------------------- | --------------------------- |
| **উদাহরণ URL**       | `/blog/my-post`               | `/blog?page=2`              |
| **Purpose**          | Resource identify করা         | Filter/Search করা           |
| **Required?**        | ✅ হ্যাঁ                      | ❌ না (Optional)            |
| **Folder Structure** | `[id]` লাগে                   | লাগে না                     |
| **SEO**              | Better                        | Moderate                    |
| **Use Case**         | Product details, User profile | Search, Filters, Pagination |

**মনে রাখবেন:**

- `params` হলো সেই তথ্য যা ছাড়া পেজটি অসম্পূর্ণ (যেমন প্রোফাইল আইডি)
- `searchParams` হলো ঐচ্ছিক তথ্য যা পেজের কন্টেন্ট ফিল্টার করতে সাহায্য করে

---
