# 🚀 Page Router vs App Router

## 1️⃣ Page Router (Pages-based Routing) 🏛️

এটি Next.js-এর ক্লাসিক বা পুরনো পদ্ধতি। এটি মূলত **Client-side Rendering (CSR)** এর ওপর বেশি গুরুত্ব দেয়।

- **ফাইল স্ট্রাকচার:** `pages/` ফোল্ডারের ভেতর আপনি যে ফাইল রাখবেন, সেটিই একটি লিঙ্ক বা রাউট হয়ে যাবে।
- **সীমাবদ্ধতা:** \* এতে **Layout** ম্যানেজ করা কঠিন। এক পেজ থেকে অন্য পেজে গেলে পুরো লেআউট রি-রেন্ডার হতে পারে।
- সব ডাটা ফেচিং মেথড (`getServerSideProps`, `getStaticProps`) শুধুমাত্র পেজ লেভেলে কাজ করে, ছোট কোনো কম্পোনেন্টের ভেতর নয়।
- ব্রাউজারে অনেক বেশি জাভাস্ক্রিপ্ট ফাইল (Bundle) পাঠাতে হয়, যা সাইটকে কিছুটা ভারী করে।

---

## 2️⃣ App Router (The Modern Era - Next.js 13+) 🌟

এটি বর্তমানে Next.js-এর স্ট্যান্ডার্ড। এটি **React Server Components (RSC)** আর্কিটেকচারের ওপর ভিত্তি করে তৈরি।

- **Nested Layouts:** এর সবথেকে বড় শক্তি। ধরুন আপনার একটি ড্যাশবোর্ড আছে যার বাম পাশে সাইডবার। `dashboard/layout.tsx` ব্যবহার করলে আপনি যখন ড্যাশবোর্ডের ভেতর বিভিন্ন পেজে যাবেন, সাইডবারটি কখনোই রি-রেন্ডার হবে না। শুধু মাঝখানের কন্টেন্ট পরিবর্তন হবে।
- **Server Components by Default:** এখানে সব কম্পোনেন্ট ডিফল্টভাবে সার্ভার কম্পোনেন্ট। অর্থাৎ, কোড সার্ভারে রান হয় এবং ব্রাউজারে শুধু রেজাল্ট (HTML) যায়। এতে ব্রাউজারের ওপর চাপ কমে এবং সাইট সুপার ফাস্ট হয়।
- **Special Files:** রাউটিং সহজ করার জন্য এখানে কিছু স্পেশাল ফাইল আছে:
- `page.tsx`: পেজের কন্টেন্ট।
- `layout.tsx`: কমন ডিজাইন (যেমন: Navbar)।
- `loading.tsx`: অটোমেটিক লোডিং স্টেট।
- `error.tsx`: এরর হ্যান্ডেলিং।

---

## 3️⃣ Bundlers: পর্দার পেছনের কারিগর 🛠️

আমরা যখন কোড লিখি, তখন শত শত ফাইল এবং লাইব্রেরি ব্যবহার করি। ব্রাউজার এতগুলো ফাইল আলাদাভাবে বুঝতে পারে না। **Bundler**-এর কাজ হলো এই সবগুলোকে গুছিয়ে ১-২টি ফাইলে রূপান্তর করা।

- **Webpack:** এটি ডাইনোসরের মতো শক্তিশালী। অনেক কনফিগারেশন করা যায়, কিন্তু বড় প্রজেক্টে এটি একটু স্লো।
- **TurboPack (Next.js-এর নিজস্ব):** Next.js এখন **Turbopack** ব্যবহার করছে (যা Webpack-এর উত্তরসূরি)। এটি Rust ভাষায় লেখা, তাই এটি অবিশ্বাস্য দ্রুত।
- **কেন দরকার?** \* **Minification:** কোড থেকে অপ্রয়োজনীয় স্পেস এবং কমেন্ট বাদ দিয়ে ফাইলের সাইজ ছোট করে।
- **Tree-shaking:** আপনি যদি একটি বিশাল লাইব্রেরি থেকে মাত্র একটি ফাংশন ব্যবহার করেন, তবে Bundler বাকি অব্যবহৃত কোডগুলো ঝেড়ে ফেলে দেয়।

---

## 4️⃣ App Router কেন ব্যবহার করবেন? (The Real Benefits) 🎯

ডেভলপাররা কেন এখন আর Page Router ব্যবহার করতে চান না, তার প্রধান কারণগুলো হলো:

1. **Zero Bundle Size (Server Components):** আপনি যদি সার্ভার কম্পোনেন্টে কোনো বিশাল লাইব্রেরি ব্যবহার করেন, তবে সেই লাইব্রেরির ১ কিলোবাইট কোডও ইউজারের ব্রাউজারে যাবে না। সব কাজ সার্ভারে শেষ হয়ে যাবে।
2. **Streaming:** পুরো পেজ রেডি হওয়ার জন্য বসে থাকতে হয় না। একটি বড় ডাটা টেবিল লোড হতে সময় লাগলে, বাকি পেজ লোড হয়ে যাবে আর টেবিলের জায়গায় লোডিং দেখাবে।
3. **Simplified Fetching:** আগে `getServerSideProps` ব্যবহার করতে হতো যা খুব ঝামেলার ছিল। এখন সরাসরি কম্পোনেন্টের ভেতর `async/await` দিয়ে `fetch` করা যায়।
4. **Colocation:** আপনি একই ফোল্ডারের ভেতর আপনার CSS, টেস্ট ফাইল এবং কম্পোনেন্ট রাখতে পারেন, যা প্রজেক্ট মেইনটেইন করা সহজ করে।

---

### 📊 Page Router বনাম App Router (সংক্ষেপে)

| বৈশিষ্ট্য      | Page Router          | App Router                |
| -------------- | -------------------- | ------------------------- |
| **ফোল্ডার**    | `pages/`             | `app/`                    |
| **রেন্ডারিং**  | Client-centric       | Server-centric (RSC)      |
| **Layout**     | কাস্টম ভাবে করতে হয়  | ইন-বিল্ট (layout.tsx)     |
| **স্পিড**      | ভালো                 | অসাধারণ (Turbopack + RSC) |
| **ডাটা ফেচিং** | `getServerSideProps` | `async/await fetch`       |

---

### 🎯 শেষ কথা

আপনি যদি নতুন কোনো প্রজেক্ট শুরু করেন, তবে অবশ্যই **App Router** শিখবেন এবং ব্যবহার করবেন। এটি শুধুমাত্র আধুনিক নয়, বরং এটিই ওয়েব ডেভলপমেন্টের ভবিষ্যৎ।

### ⬅️ Previous / Next ➡️

| ⬅️ Previous                                                      |                             Next ➡️ |
| :--------------------------------------------------------------- | ----------------------------------: |
| [03 - ⬅️ Next.js Features Deep Explanation](./module-01/C-03.md) | [05 - Next ➡️](./module-01/C-05.md) |
