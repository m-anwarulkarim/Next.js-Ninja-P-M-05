# 🔑 Next.js Features Deep Explanation

---

## 1️⃣ Routing Pattern (রাউটিং প্যাটার্ন) 🗂️

Next.js-এর **App Router** সিস্টেমটি ফোল্ডার স্ট্রাকচারের ওপর ভিত্তি করে চলে।
এখানে প্রতিটি রাউট একটি ফোল্ডার এবং তার ভেতর একটি `page.tsx` ফাইল।

### 🔹 Routing Types

- **Static Route**
  সাধারণ পেজ যেমন `/about`, `/contact`।
  শুধু ফোল্ডার বানিয়ে `page.tsx` দিলেই রাউট তৈরি।

- **Dynamic Route `[slug]`**
  এটি খুবই গুরুত্বপূর্ণ।
  যখন আপনি হাজার হাজার প্রোডাক্ট বা ব্লগ পেজ বানাবেন, তখন প্রতিটির জন্য আলাদা ফাইল না করে একটি ফাইল `[slug]` দিয়ে সব হ্যান্ডেল করা হয়।
  যেমন:
  `/blog/1`, `/blog/react` — সব এই এক ফাইল থেকেই জেনারেট হবে।

- **Catch-All Route `[...slug]`**
  এটি একধাপ এগিয়ে।
  এটি শুধু একটি প্যারামিটার নয়, বরং ইউআরএল-এর পরবর্তী সব অংশ ধরবে।
  যেমন:
  `/docs/v1/auth/login` — এই পুরো পথটি একটি ফাইল থেকেই ডাটা পাবে।

- **Route Group `(folderName)`**
  অনেক সময় আমরা প্রজেক্ট সাজানোর জন্য ফোল্ডার করি কিন্তু চাই না সেই ফোল্ডারের নাম URL-এ আসুক।
  যেমন:
  `(dashboard)/settings` দিলে ইউআরএল হবে শুধু `/settings`।

- **Parallel Routes `@folder`**
  একই স্ক্রিনে একাধিক পেজ দেখানোর জন্য।
  যেমন: একই সাথে `Dashboard` এবং `Sidebar` আলাদা আলাদা রাউট থেকে লোড হবে।

- **Intercepting Routes**
  এটি চমৎকার একটি ফিচার।
  ধরুন ফেসবুকে একটি ছবিতে ক্লিক করলে সেটি একটি **Modal** হিসেবে ওপেন হয় (URL পরিবর্তন হয় কিন্তু পেজ রিফ্রেশ হয় না)।
  এটিই হলো ইন্টারসেপ্টিং রাউট।

---

## 2️⃣ Rendering Techniques (রেন্ডারিং টেকনিক) 🎨

Next.js আপনাকে নমনীয়তা দেয় যে আপনি আপনার ডাটা কখন এবং কোথায় রেন্ডার করবেন।

- **Server-Side Rendering (SSR)**
  প্রতিটি রিকোয়েস্টে সার্ভার ডাটা তুলে এনে HTML তৈরি করে ব্রাউজারে পাঠায়।
  এটি রিয়েল-টাইম ডাটা (যেমন: স্টক মার্কেট বা প্রোফাইল পেজ) এর জন্য সেরা।

- **Static Site Generation (SSG)**
  বিল্ড টাইমে একবারই পেজ তৈরি হয়।
  এটি সবথেকে দ্রুত কারণ সার্ভারকে বারবার কাজ করতে হয় না।
  ব্লগ বা ডকস এর জন্য এটি আদর্শ।

- **Incremental Static Regeneration (ISR)**
  এটি ম্যাজিকের মতো।
  আপনার সাইট স্ট্যাটিক থাকবে কিন্তু আপনি চাইলে প্রতি ১০ বা ৬০ সেকেন্ড পর পর ব্যাকগ্রাউন্ডে ডাটা আপডেট করে নিতে পারবেন।
  এতে রি-বিল্ড ছাড়াই সাইট আপডেট থাকে।

- **Client-Side Rendering (CSR)**
  এটি সাধারণ React-এর মতো।
  যখন ইউজার ইন্টারঅ্যাকশন (যেমন: ফর্ম ইনপুট, ক্লিক) দরকার হয়, তখন আমরা `"use client"` ব্যবহার করি।

- **Streaming & Suspense**
  পুরো পেজ লোড হওয়ার জন্য অপেক্ষা না করে, যে অংশটুকু রেডি হয়েছে তা আগে দেখায়।
  যেমন:
  একটি বড় পেজের টেক্সট আগে আসবে, আর ভারী ছবি বা চার্ট পরে `"Loading..."` অবস্থা থেকে আসবে।

---

## 3️⃣ Layout, Loading, Error (The Magic Files) 🧩

Next.js-এ কিছু ফাইল সংরক্ষিত (Reserved) যা অটোমেটিক ফিচার দেয়।

- **layout.tsx**
  এটি একটি কমন স্ট্রাকচার।
  এখানে Navbar/Footer রাখলে আপনি যখন পেজ চেঞ্জ করবেন, এই অংশগুলো রি-রেন্ডার হবে না, যা পারফরম্যান্স বাড়ায়।

- **loading.tsx**
  কোনো পেজ লোড হওয়ার সময় ইউজারকে কী দেখানো হবে (Skeleton বা Spinner), তা এখানে থাকে।

- **error.tsx**
  যদি কোনো পেজে এরর হয়, তবে পুরো সাইট ক্রাশ না করে শুধু ওই পেজের বদলে একটি সুন্দর Error Message দেখাবে।

- **not-found.tsx**
  ইউজার ভুল লিঙ্কে গেলে অটোমেটিক এই পেজটি চলে আসে।

---

## 4️⃣ API Routes & Server Actions 🛠️

Next.js শুধু ফ্রন্টএন্ড নয়, এটি একটি পূর্ণাঙ্গ ব্যাকএন্ড ফ্রেমওয়ার্ক।

- **API Routes**
  `app/api/` ফোল্ডারে ফাইল লিখে আপনি নিজের API তৈরি করতে পারেন।
  আলাদা কোনো Node.js বা Express সার্ভার দরকার হয় না।

- **Server Actions (Next.js 14+)**
  এটি আধুনিকতম পদ্ধতি।
  সরাসরি ফ্রন্টএন্ড কম্পোনেন্ট থেকে সার্ভারের ফাংশন কল করা যায়।
  ফর্ম সাবমিট করা বা ডাটাবেসে ডাটা সেভ করা এখন অনেক সহজ।

---

## 5️⃣ Data Fetching Patterns 📡

- **Server Fetching**
  ডাটা সরাসরি সার্ভারে ফেচ হয়, তাই API Key বা সিক্রেট ডাটা ব্রাউজারে লিক হওয়ার ভয় থাকে না।

- **Parallel Fetching**
  ধরুন আপনার ২টি API আছে।
  আপনি `Promise.all` ব্যবহার করে দুটোই একসাথে লোড করতে পারেন, যা সময় বাঁচায়।

---

## 6️⃣ Caching 🧠

Next.js-এর সবথেকে শক্তিশালী দিক হলো এর ক্যাশিং সিস্টেম।
একবার ডাটা লোড হলে তা মেমরিতে রেখে দেয় যাতে দ্বিতীয়বার দ্রুত সার্ভ করা যায়।
আপনি চাইলে নির্দিষ্ট সময় পর এই ক্যাশ ক্লিয়ার (Revalidate) করতে পারেন।

---

## 7️⃣ Optimization ⚡

- **Image Optimization**
  ছবির সাইজ অটোমেটিক ছোট করে এবং শুধু যখন স্ক্রিনে আসে তখন লোড (Lazy Load) করে।

- **Font Optimization**
  গুগল ফন্ট ব্যবহার করলে লেআউট শিফট (Layout Shift) হয় না।

- **Prefetching**
  কোনো লিংকের ওপর মাউস নিলেই বা লিংকটি স্ক্রিনে আসলেই Next.js সেই পেজটি ব্যাকগ্রাউন্ডে লোড করে রাখে।
  ফলে ক্লিক করার সাথে সাথে পেজ চলে আসে।

---

## 8️⃣ Metadata & SEO 🔍

Next.js-এ SEO করা পানির মতো সহজ।
আপনি প্রতি পেজের জন্য আলাদা মেটা টাইটেল, ডেসক্রিপশন এবং সোশ্যাল মিডিয়া প্রিভিউ ইমেজ (Open Graph) সেট করতে পারেন।
এটি গুগল সার্চে আপনার সাইটকে সবার উপরে নিয়ে আসতে সাহায্য করে।

---

## 🎯 উপসংহার

Next.js মূলত একটি **All-in-one** সমাধান।
এটি আপনাকে পারফরম্যান্স নিয়ে ভাবতে দেয় না, কারণ এর ভেতরেই সেরা অপ্টিমাইজেশন করা থাকে।
আপনি যদি আধুনিক ওয়েব অ্যাপ্লিকেশন বা ই-কমার্স সাইট বানাতে চান, তবে **Next.js-এর কোনো বিকল্প নেই**।

### ⬅️ Previous / Next ➡️

| ⬅️ Previous                                                      |                                                  Next ➡️ |
| :--------------------------------------------------------------- | -------------------------------------------------------: |
| [02 - ⬅️ Why Next.js is the Best Framework](./module-01/C-02.md) | [04 - Page Router vs App Router ➡️](./module-01/C-04.md) |
